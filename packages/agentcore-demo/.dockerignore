# Keep the Docker build context (and CDK asset staging, which honors this
# file) down to what the Dockerfile actually COPYes: deploy/.
# cdk.out especially — staging it recurses into its own output (ENAMETOOLONG).
cdk
node_modules
**/node_modules
lambda
workspace
index.html
favicon.ico
auth-server.js
README.md
