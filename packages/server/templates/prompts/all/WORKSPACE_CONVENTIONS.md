### Workspace `.halo/tmp/`
Runtime intermediates tied to the task (generated artifacts, saved shell
output, downloaded media, research notes) go in `<workspace>/.halo/tmp/`
— referenceable by path in reports and later sessions. OS `/tmp` is for
genuinely throwaway scratch only.
